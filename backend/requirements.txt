fastapi>=0.116.0,<1.0.0
uvicorn[standard]>=0.35.0,<1.0.0
markitdown[pdf,pptx,xlsx,xls]>=0.1.6,<0.2.0
numpy>=1.24
# 环境变量加载与表单/文件上传解析
python-dotenv>=1.0
python-multipart>=0.0.20
# 认证（阶段1启用）：密码哈希 + JWT + 凭据加密
passlib[bcrypt]>=1.7
# passlib 1.7 与 bcrypt>=4.1 不兼容（缺 __about__），锁定 4.0.x
bcrypt<4.1
email-validator>=2.0
PyJWT>=2.8
cryptography>=42.0
# FastAPI/Starlette TestClient（当前 starlette 版本需要 httpx2 包）
httpx2>=0.28
# 扫描版 PDF / 图片本地 OCR 级联（可选，缺失时自动跳过并提示安装）
pymupdf>=1.24
rapidocr>=3.6,<4.0
