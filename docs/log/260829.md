# 2026-08-29 开发日志

## 会话 3：AI 协作文档框架（new_docs/）建立

**背景**：用户决策项目进入学习/冻结期（暂不增不改功能），两件事：①认识项目 ②建立
新的 AI 协作开发框架。本会话完成 ②，①的配套文档（架构解析+学习指南）已在本日早前
会话产出。

**做了什么**：

1. 讨论并确定框架形态：用户提出 docs.md 总规则 + project/study 双库 + 每文件夹同名
   总文档 + log 按天记录 + design/designs-specs 双文档 + ideas 新功能池。AI 补充三件事：
   - 模块划分从"UI 容器"改为**功能域平铺**（modules/ 20 域 + core/ 9 域）——避免同一
     后端域被多个 UI 文件夹重复描述；UI 直觉保留为 sidebar-shell/topbar/right-panel
     三个壳域 + modules.md 的 UI 挂载对照表
   - 未想好的"优化/系统级更新放哪" → **proposals/ 变更提案区**（ideas→proposal→实施→
     结论回流 design→归档 的完整生命周期，补上原框架"design 落地后没人更新"的缺口）
   - study/model → **patterns/**（概念→项目实例对照库；避开"模型"一词在本项目的语义占用）
   - 目录名英文（kebab-case）：项目已有 uvicorn --reload 中文路径失效 Bug，不往非 ASCII
     雷区再加码；文档内容全中文
2. 用户三项拍板（AskUserQuestion）：功能域平铺 ✓、英文目录名 ✓、全骨架+2~3 样板域 ✓
3. 脚手架落地：删除用户手建的旧空骨架（model/project_log/study_log 等，全 0 字节无损失；
   其中"划词→添加到笔记/课程意见反馈"的细粒度被吸收为 notes/selection/course-feedback 三域），
   建成 29 域骨架（每个占位 design.md 一句话定位）
4. 写文档：docs.md（宪法：三层级/两回流/双文档模式/生命周期/人AI权限表/命名对照）；
   project.md（架构参考+域地图）；study.md；各子文件夹总文档（log/ideas/proposals/
   modules/core/guide/patterns 各一份）
5. **三个样板域**正式起草（AI 逆向起草，待用户审定）：core/model-client、
   modules/practice、modules/settings——各含 design.md（决策/不变量/禁区/异议区）+
   designs-specs.md（导读含典型调用链/文件清单/入口/数据契约/测试锚点）。所有代码
   锚点经本会话 grep/Read 核实（行号锚定 2026-08-29 快照）
6. 历史文档迁移（git mv 保留历史）：学习指南→study/guide/；课程生成效率优化方案→
   proposals/（活跃）；6 篇已落地方案→proposals/archive/；DEV_BOARD.md→project/。
   《项目架构解析.md》暂存 new_docs 根（内容已改写进 project.md，原件待用户确认后删）
7. CLAUDE.md 最小补丁（2 处）：开头引用改指 new_docs/docs.md + 看板新路径；会话流程
   增加"写当日 log"义务。CLAUDE.md 本体留根目录（自动加载机制决定）

**关键决策及理由**：

- 双文档模式兼容两阶段：已有模块走"考古模式"（AI 从代码逆向起草），新功能走"规划模式"
  （先 specs 后实现）——项目已开发大半，两种模式都会长期并存
- specs 冲突以代码为准、design 冲突必须回流人裁决——防止 AI 悄悄改设计迁就代码
- 全量空壳但不批量起草——原框架作者"specs 写不过来也审不过来"的教训；域文档随学习
  进度生长，每域 design 是该域学习的毕业考

**风险与遗留**：

- 《项目架构解析.md》原件还在 new_docs 根（`docs_项目架构解析_待并入project.md`），
  内容已并入 project.md，待用户确认后删除
- AGENTS.md 等其他 agent 约束文件可能引用旧 docs/ 路径，未检查（下次会话顺手）
- `how v2.0.0` 未跟踪文件仍在工作区（非本会话产物，不动）

**下一步**：用户验收本框架（重点：docs.md 权限表、三篇样板 design）→ 开始学习指南
阶段 0/A → 学完阶段 B 触发 model-client 域 design 审定

## 会话 4：bugs 错误档案库 + 库更名 docs（框架 v1.1）

**用户两项任务**：① AI 建 bugs 模块（遇错自动记录：时间/错误/后果/建议修复）②用户自行迁移
course-style-lab 至 draft/ 并将 new_docs 更名为 docs（AI 只登记）。

**做了什么**：

1. 新建 `project/bugs/`：bugs.md 总文档（规则+档案模板+索引）+ 12 个错误档案（从看板 🐛 区
   迁移建档：2 个 🟡 已修待验证、3 个 🔴 待定位/待清理、7 个 ✅ 已关闭含 commit）。
   核心机制：**AI 遇错自动建档义务**（写入宪法 §4.3）；bugs/ 全量持久 vs 看板只留活跃条目；
   状态流转 🔴→🟡→✅/⚪；误报在原档案更正不删
2. 同步执行了用户任务 2 的登记部分（目录部分用户已自行完成）：发现用户已把 new_docs 更名
   为 docs 并迁入 course-style-lab → 将 bugs/ 从 new_docs 残留路径搬入 docs/project/ →
   全库清理 new_docs 引用（CLAUDE.md / AGENTS.md / docs.md / 看板 / 1 个 bug 档案，
   本 log 保留历史原话不改）
3. 宪法升 v1.1：结构图加 bugs/ 与 draft/；§4.3 bugs 规则；§5 权限表加 bugs 建档行；
   §6 更名说明；§8 迁移表全部置 ✅；§9 版本记录
4. 看板快照更新

**关键决策及理由**：

- bugs/ 与看板 🐛 区双层分工而非互相替代：看板是状态视图（会刷新、有淘汰），bugs 是
  全量档案（只增不删）——和 log/board 分工同构
- 档案首批从看板迁移而非从零积累：12 个 bug 的根因/修复记录是项目最宝贵的工程知识，
  立即入档防止随看板 30 条滚动淘汰而丢失

**勘误**（会话 3 遗留项）：《项目架构解析》原件仍在库根
（`docs/docs_项目架构解析_待并入project.md`），待用户确认删除；AGENTS.md 引用已在本会话
随批量替换同步，会话 3 的"未检查"遗留项就此关闭。

