# 学习路线记录

> 由 AI 按学习路线维护；这是路线与课堂导航，不代替用户学习心得。

## 当前状态

- 阶段：准备阶段，尚未进入阶段 A。
- 状态：阶段 0 全局地图补强中。
- 当前目标：通过实际阅读和数据观察，理解后台 job、SQLite、workspace.json 和统一模型调用出口。

## 会话 2026-08-30：后台 job 与双存储预备课

### 实际代码锚点

- `backend/app/agent_runtime.py:562-606`：`_claim_job`，领取 queued job 并改为 running。
- `backend/app/agent_runtime.py:609-647`：完成与失败落库；失败可能重新排队或最终失败。
- `backend/app/agent_runtime.py:650-689`：续租、所有权检查和心跳。
- `backend/app/agent_runtime.py:691-764`：`AgentJobWorker` 与 `_run` 主循环。
- `backend/app/workspace.py:42-70`：课程目录和 workspace.json 路径。
- `backend/app/workspace.py:213-271`：空课程 workspace 的初始结构。
- `backend/app/workspace.py:335-386`：`load_workspace`。
- `backend/app/workspace.py:389-427`：锁、revision、原子保存和 `save_workspace`。

### 已形成的初步理解

- job 是先登记、后由 Worker 执行、可查询状态和结果的持久任务。
- SQLite 保存结构化小数据、队列和运行记录；workspace.json 保存课程复杂内容正文。
- `model_client.py` 是 AI 请求的统一出口；目前只掌握职责，尚未逐行阅读。

### 待完成观察

1. 根据代码画出 queued → running → completed/failed 状态图。
2. 阅读 `get_agent_job`，确认 `agent_jobs` 表字段如何转成返回 JSON。
3. 打开测试课程的 SQLite 数据，观察 `courses`、`agent_jobs`、`agent_runs`、`artifacts`。
4. 打开测试课程的 `workspace.json`，记录顶层字段、第一项 task、studyGuide、revision 和 onboarding.status。
5. 用户完成复述后，更新对应 patterns 的状态。

### 关联文档

- [后台任务与持久队列](../patterns/后台任务与持久队列.md)
- [读改写与双存储](../patterns/读改写与双存储.md)
- [学习日志](../log/2026-08-30.md)