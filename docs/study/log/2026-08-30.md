# 2026-08-30 学习笔记

## 会话：从全局地图补强后台任务与双存储

### 今天学了什么

- 暂未进入阶段 A；先补强三个基础概念：后台 job、SQLite 与 workspace.json 双存储、model_client.py 的统一模型调用出口。
- 后台 job 的代码入口：backend/app/agent_runtime.py:691 的 AgentJobWorker；_run 在 709 行；_claim_job 在 562 行；_renew_job_lease 在 650 行；has_agent_job_ownership 在 665 行。
- workspace 的代码入口：backend/app/workspace.py；_workspace_path 在 46 行，load_workspace 在 335 行，save_workspace 在 399 行。
- 当前理解：SQLite 主要保存结构化小数据、队列与运行记录；workspace.json 主要保存一门课的复杂内容正文。
- 当前理解：model_client.py 是 AI 出网统一出口，集中处理请求、流式响应、首 token 超时、重试、主备切换和用量记录。

### 卡壳与想通的地方

- 原先不理解“后台 job”；现在先把它理解成写入 SQLite 的持久任务单，由 AgentJobWorker 异步消费，前端另行查询进度。
- 原先不了解 SQLite 和 JSON；已经建立“系统管理信息”和“课程正文”的分工，但还没有亲自打开数据库与 workspace 文件观察真实数据，尚未算过关。
- 原先看不懂 model_client.py；目前只掌握职责，不要求现在逐行理解。

### 下一步

- 实际阅读 agent_runtime.py 562-765 行，画出一次 job 从 queued 到 completed/failed 的状态变化。
- 阅读 workspace.py 42-70、335-427 行，并打开一门测试课程的 workspace.json，把代码字段和真实 JSON 对照。
- 继续追问 SQLite 表结构与一次任务在 agent_jobs、agent_runs、artifacts 中的关系。
- 暂不进入阶段 A。

> 本条由用户明确要求 AI 协助记录。按 study.md 与 log.md，学习笔记原则上用户执笔为主，AI 只有在用户要求时协同整理。
