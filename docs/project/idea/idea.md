# idea

idea 阶段模块的 spec/plan 暂存目录：尚无正式 design.md 的模块，其 brainstorming 设计记录与实施计划先放这里（`idea/<模块>/YYYY-MM-DD-<主题>-design.md`）。

**使用方式**：模块正式立项（design.md 迁入 modules/ 正式域目录）时，其 spec/plan 一并迁入（机制见 `../README.md`）。

**边界**：本目录权属开发者；AI 只动自己产出的 spec/plan 文件，不碰开发者放置的任何内容。
