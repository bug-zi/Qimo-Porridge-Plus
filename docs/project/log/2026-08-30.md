# 2026-08-30 开发日志

## 会话 1：《项目架构解析》并入 project.md + 宪法新增「AI 生成语言规范」

**背景**：用户指示两件事：①把库根暂存的《项目架构解析》正式并入 project.md 并删原件
（2026-08-29 会话3 的遗留项，原件此前一直待确认）；②在 docs.md 新增一个
"AI 生成语言规范"部分，作为用户手动存放文风规范、AI 交互时遵守的专区。

**做了什么**：

1. **数字核实先行**：逐项实测代码快照，发现原架构解析多处漂移——
   agents/ 实为 19 个业务文件（原文 16）、ModuleView useState 实测 104（原文 108）、
   auth 三文件合计 458 行（原文 371）、routers 12 个业务 router 属实、
   各域模块行数全部与原文一致。并入时全部按 2026-08-30 实测修正。
2. **project.md 重写合并**：以架构解析的详细骨架为主干（模块地图/调用链/四条链路解剖
   完整保留），原 project.md 的域地图收编为 §8。**刻意保持 §3.4（模型调用链图）与
   §4.1（课程生成链路）的章节号不变**——学习指南有 5 处锚点引用这两个章节号。
3. **引用同步**：全库 Select-String 复查《架构解析》引用（先前 Grep 中文模式漏报，
   改用 PowerShell 才查全），学习指南 7 处引用改指 project.md；docs.md §8 迁移表
   该行状态改为"已并入并删除原件"。
4. **删除原件**：`docs/docs_项目架构解析_待并入project.md` 已删（git 可溯源）。
5. **docs.md 宪法 v1.1→v1.2**：新增 §6「AI 生成语言规范」——用户执笔专区（AI 不得
   改写/删除/预填条目，只读遵守），插入位置在 §5 人/AI 权限表之后（本质同属 AI 行为
   约束），权限表同步加一行"语言规范条目：✅ 用户专属执笔 / ❌ AI 只读遵守可提议"。
   后续章节 6→7→8→9 顺延重编号为 7→8→9→10。
6. **顺手修正**：docs.md §1"18 个 modules"实为 20（对照表自身就列 20 行）。
7. 看板快照/已完成区更新，本日志建档。

**关键决策及理由**：

- **学习指南锚点优先于章节美观**：合并后的 project.md 若重排章节号，学习指南的
  "§3.4 的那张图""§4.1"引用全部失效。选择让新结构适配旧锚点（域地图放 §8）而非
  反过来——学习指南是用户正在使用的学习路线文档，不能因整理而断链。
- **语言规范设为"用户执笔专区"而非普通协作区**：用户明说"我自己手动编写存入"。
  参照 ideas/ 的先例（用户专属，AI ❌）而非 designs-specs.md（AI 职责）。这是文风
  宪法，裁判权必须在人；AI 的角色被明确定义为"只读遵守 + 会话中口头提议"。
- **规范区宁空勿假**：当前零条目，但 AI 不得替用户起草"看起来合理"的默认规范——
  那会稀释用户专区属性，等用户真写时反被锚定。空着就是正确状态。

**发现的问题**：Grep 工具对中文文件内容（UTF-8）模式匹配漏报（查"架构解析"返回
No matches，实际全库十余处）——本会话改用 PowerShell Select-String 才查全。
后续涉及中文内容检索时注意工具选择。

**下一步**：用户自行在 docs.md §6 规范区填写语言条目；此后 AI 文档工作交互即受其约束。

## 会话 2：学习文档三层编排方案待审

用户提出：路线推进材料放 guide，概念与项目实例对照放 patterns，个人总结心得放 study log，并要求优化 docs.md 规则。已研究现有学习文档并起草 `docs/project/proposals/学习文档编排与规则优化方案.md`。方案建议新增 `guide/学习路线记录.md`，新增后台队列与双存储两篇 pattern，并明确 log 仅在用户授权时由 AI 协助整理。方案已保存，等待用户审阅；用户随后确认按方案实施。已完成 docs.md §8、study.md、guide.md、patterns.md 规则同步，新增学习路线记录与后台 job/双存储两篇 pattern；未改功能代码。
